BITS 16
ORG 0x8000

start:
    cli

    xor ax, ax
    mov ds, ax
    mov es, ax

    sti

    mov si, msg

.print:
    lodsb
    or al, al
    jz halt

    mov ah, 0x0E
    int 0x10

    jmp .print

halt:
    jmp $

msg db "STAGE2 OK", 0